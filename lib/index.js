/**
 * widget-dock — host (node) half.
 * Pure UI plugin: an empty apply exists so the package appears in the host
 * cordis Loader; the browser half ships via exports["./client"].
 */
export function apply() {}
